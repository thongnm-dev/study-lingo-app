abstract class AppConstants {
  static const String appName = 'StudyLingo';
  static const String apiBaseUrl = 'https://api.studylingo.local';
  static const Duration httpConnectTimeout = Duration(seconds: 30);
  static const Duration httpReceiveTimeout = Duration(seconds: 30);
}
