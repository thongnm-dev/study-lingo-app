import 'package:flutter/material.dart';

/// Brand colors referenced by `AppTheme`. The Material 3 ColorScheme is derived
/// from [brandSeed]; any hand-picked accents live here so feature widgets don't
/// hardcode hex literals.
abstract class AppColors {
  static const Color brandSeed = Color(0xFF3A6FF8);
}
